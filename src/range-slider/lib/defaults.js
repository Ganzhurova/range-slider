import types from './constants';

const defaults = {
  type: types.SINGLE,
  vertical: false,
  label: false,
  scale: false,
  min: 0,
  max: 100,
  from: 50,
  to: 0,
  step: 25,
};

export default defaults;
