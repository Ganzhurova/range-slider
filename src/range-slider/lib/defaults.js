import types from './constants';

const defaults = {
  type: types.SINGLE,
  vertical: false,
  label: false,
  scale: false,
  min: 0,
  max: 100,
  from: 50,
  to: 75,
  step: 5,
};

export default defaults;
