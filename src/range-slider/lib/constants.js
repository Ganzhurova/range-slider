const types = {
  SINGLE: 'single',
  DOUBLE: 'double',
};

const size = {
  WIDTH: 'width',
  HEIGHT: 'height',
};

const directions = {
  LEFT: 'left',
  TOP: 'top',
};

const coordNames = {
  X: 'x',
  Y: 'y',
};

const positionIndex = {
  0: 'from',
  1: 'to',
};

const dragDirections = {
  FORWARD: 'forward',
  BACKWARD: 'backward',
};

export { types, size, directions, coordNames, positionIndex, dragDirections };
