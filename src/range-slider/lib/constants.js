const types = {
  SINGLE: 'single',
  DOUBLE: 'double',
};

const size = {
  WIDTH: 'width',
  HEIGHT: 'height',
};

const directions = {
  LEFT: 'left',
  TOP: 'top',
};

export { types, size, directions };
