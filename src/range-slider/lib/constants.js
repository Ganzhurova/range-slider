const types = {
  SINGLE: 'single',
  DOUBLE: 'double',
};

const size = {
  WIDTH: 'width',
  HEIGHT: 'height',
};

const directions = {
  LEFT: 'left',
  TOP: 'top',
};

const coordNames = {
  X: 'X',
  Y: 'Y',
};

const positionIndex = {
  0: 'from',
  1: 'to',
};

export { types, size, directions, coordNames, positionIndex };
